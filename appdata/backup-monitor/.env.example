# Backup Monitor Configuration
# Copy this file to .env and customize as needed
# For Docker Compose, set these in docker-compose.yml instead

# How often to check for new metrics (in hours)
# Supports decimals: 0.5 = 30 minutes, 0.083 = 5 minutes
# Default: 6
IMPORT_INTERVAL_HOURS=6

# How long to keep metrics in database (in days)
# Default: 90
RETENTION_DAYS=90

# Path to SQLite database file
# Default: /data/backups.db
DB_PATH=/data/backups.db

# Path to JSONL metrics input file
# Default: /data/metrics.jsonl
METRICS_FILE=/data/metrics.jsonl

# Timezone (system-level, affects container)
# Default: UTC
# TZ=America/Los_Angeles
